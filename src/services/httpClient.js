// src/services/httpClient.js

// Placeholder: nanti bisa diganti axios/fetch ke backend beneran
export async function get(url, options = {}) {
  // Di sini kamu bisa tambahkan interceptor, auth token, dsb
  // Untuk dummy sekarang, kita gak pakai.
  throw new Error("httpClient.get belum diimplementasi ke backend");
}
